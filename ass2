//program to print given year is leap year or not//
#include<stdio.h>
void main()
{
	int year;
	scanf("%d",&year);
	if(year%4==0 && year%100!=0 || year%400==0)
	{
		printf("given year %d is leap year",year);
	}
	else
	{
		printf("given year %d is not a leap year",year);
	}
}
